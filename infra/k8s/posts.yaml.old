apiVersion: v1
kind: Pod
metadata:
    name: posts
spec:
    containers:
        - name: posts
          image: vectornull/posts:0.0.1
          # This policy will ensure that Kubernetes will use the image build locally from you image cache and not pull from a registry.
          imagePullPolicy: Never

          # run <minikube start --driver=virtualbox> if you are using virtualbox

          # run <kubectl apply -f posts.yaml> to create
